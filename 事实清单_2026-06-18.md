# 允物 ERP · 运行环境事实清单

> 2026-06-18 15:21 CST
> 原则：只列可直接验证的事实，不推测

---

## F1 — .env 文件中的 DATABASE_URL

**文件**：`/Users/ryan/yunwu-brand-os/.env`

```
DATABASE_URL="file:./prisma/yunwu.db"
NEXTAUTH_SECRET="Lyny7V00l6HIG/gp7uRs/dl0/DVSAvy2jMeTZhkOLQs="
NEXTAUTH_URL="http://localhost:3000"
```

→ DATABASE_URL 指向本地文件 `prisma/yunwu.db`。协议为 `file:`。

---

## F2 — schema.prisma 当前 datasource provider

**文件**：`/Users/ryan/yunwu-brand-os/prisma/schema.prisma`（第 5-8 行）

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
```

→ 当前磁盘上的 `schema.prisma` 的 provider 是 `sqlite`。

---

## F3 — schema.pg.prisma 当前 datasource provider

**文件**：`/Users/ryan/yunwu-brand-os/prisma/schema.pg.prisma`（第 5-8 行）

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

→ 存在一个备用文件 `schema.pg.prisma`，provider 为 `postgresql`。两个文件模型定义完全一致（均为 307 行）。

---

## F4 — vercel-build 脚本

**文件**：`/Users/ryan/yunwu-brand-os/package.json` 第 7 行

```json
"vercel-build": "cp prisma/schema.pg.prisma prisma/schema.prisma && prisma generate && next build"
```

→ Vercel 构建时执行顺序：
1. 将 `schema.pg.prisma`（provider=postgresql）复制覆盖 `schema.prisma`（provider=sqlite）
2. `prisma generate` → 生成 PostgreSQL 版 Prisma Client
3. `next build` → 使用 PostgreSQL 版 Prisma Client 编译

---

## F5 — next.config.ts 配置

**文件**：`/Users/ryan/yunwu-brand-os/next.config.ts`

```
serverExternalPackages: ["better-sqlite3"]
```

→ `better-sqlite3` 被配置为 serverExternalPackage。此包仅在 SQLite 环境需要，PostgreSQL 环境不需要。

---

## F6 — Vercel 生产环境变量名

执行 `vercel env ls` 输出：

```
DATABASE_URL       Encrypted    Production
NEXTAUTH_SECRET    Encrypted    Production
NEXTAUTH_URL       Encrypted    Production
```

→ Vercel 生产环境已设置 DATABASE_URL。值为加密状态，无法从命令行直接读取明文。

---

## F7 — Neon PostgreSQL 项目存在确认

执行 `neonctl projects get cold-sun-68601240` 输出：

```
Id:                 cold-sun-68601240
Name:               yunwu-brand-os
Region Id:          aws-us-east-2
Created At:         2026-06-18T06:53:05Z
```

→ 一个名为 `yunwu-brand-os` 的 Neon PostgreSQL 项目确实存在，区域为 `aws-us-east-2`。

创建时的连接 URI（出自会话记录）：
```
postgresql://neondb_owner:***@ep-polished-unit-ajk5rq34.c-3.us-east-2.aws.neon.tech/neondb
```

---

## F8 — 生产站点可访问

```
curl https://erp.yunwuorigin.com → HTTP 307（重定向到 /login）
curl https://erp.yunwuorigin.com/login → HTTP 200（登录页面正常返回）
```

→ 生产站点在线，登录页正常渲染。

---

## F9 — 本地 SQLite 数据库文件

```
路径: /Users/ryan/yunwu-brand-os/prisma/prisma/yunwu.db
大小: 132 KB
```

→ 本地有 SQLite 数据库文件存在。

---

## F10 — Prisma 迁移记录

```
migrations/
  20260618051429_init_v3/
  20260618053752_add_user_profile/
  20260618054532_add_reset_token/
  20260618063208_add_customer_order/
```

→ 共 4 次迁移，均为 SQLite 语法。

---

## F11 — vercel.json

→ 不存在。项目根目录无 `vercel.json` 文件。

---

## F12 — 无法验证但可推断的事项

以下无法直接验证（因为 Vercel 环境变量已加密、无法读取远端运行时的 Prisma Client），但可根据上述事实推导：

| 事项 | 推导依据 |
|------|----------|
| Vercel 生产是否使用 SQLite？ | **否**。SQLite 文件不存在于 Vercel Serverless 文件系统中（只读/临时），且 `vercel-build` 脚本将 schema 切换为 PostgreSQL |
| Vercel DATABASE_URL 值是否指向 Neon？ | 存在同名 Neon 项目 (`yunwu-brand-os`)，且部署时通过 `-e` 传入了该连接串。但无法读取加密值确认 |
| Neon 数据库中是否有数据？ | 执行过 `prisma db push` 和 `seed-admin` 脚本。admin 用户应存在。但未在本次审计中通过客户端连接验证 |
